# Day12：主循环 main loop、多周期运行与状态机连续转移验证

## 1. 学习目标

本项目用于学习嵌入式 C 工程中的主循环思想，并通过多周期运行验证状态机的连续状态转移过程。

相比 Day11 中程序只执行一次状态机更新，Day12 使用 `for` 循环模拟嵌入式系统中的主循环，使系统能够连续运行多个周期，并观察状态机在不同传感器数据下的状态变化。

Day12 的核心目标是验证以下状态转移过程：

```text
INIT -> RUN
RUN keep
RUN -> FAULT
FAULT keep
FAULT -> RUN